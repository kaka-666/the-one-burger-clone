# The One Burger 网站复刻 — 项目说明

> 1:1 复刻 [theoneburgerbcn.com](https://www.theoneburgerbcn.com/) 品牌官网  
> 技术栈：**React 19 + Vite + Tailwind CSS 4 + GSAP**

---

## 一、项目简介

本项目是对 The One Burger 巴塞罗那品牌官网的前端复刻，采用与原站相同的单页长滚动结构，包含加载动画、视频背景、GSAP 滚动驱动、汉堡展示、技术规格、包装动画、FAQ、页脚计时器等完整交互与视觉区块。

**项目路径：**

```
D:\APPdata\VSCodeData\the-one-burger-clone
```

---

## 二、本地运行

在项目根目录执行：

```powershell
cd D:\APPdata\VSCodeData\the-one-burger-clone
npm install
npm run dev
```

终端出现 `Local: http://localhost:5173/` 后，用浏览器打开该地址。

### 其他命令

| 命令 | 说明 |
|------|------|
| `npm run build` | 生产环境构建，输出到 `dist/` |
| `npm run preview` | 预览构建结果 |
| `npm run lint` | ESLint 检查 |
| `npm run download-assets` | 从原站下载字体、图片、视频到 `public/` |

### 常见问题

| 现象 | 解决方法 |
|------|----------|
| 白屏 | 必须通过 `npm run dev` 访问，不要直接双击 `index.html` |
| 端口不是 5173 | 查看终端实际输出的 `Local:` 地址 |
| 视频/图片缺失 | 运行 `npm run download-assets` 下载静态资源 |
| 滚动动画异常 | 等待加载动画结束后再滚动；刷新页面 |

---

## 三、项目结构

```
the-one-burger-clone/
├── index.html              # 网页入口
├── package.json            # 依赖与脚本
├── vite.config.js          # Vite 配置
├── vercel.json             # Vercel 部署配置
│
├── public/                 # 静态资源
│   ├── fonts/              # Kunst 字体（4 个 .otf）
│   ├── images/             # 汉堡、Logo、门店图等
│   ├── videos/             # Hero、收据、分解、包装等视频
│   └── icons/              # 配送平台 SVG 图标
│
├── scripts/
│   └── download-assets.mjs # 资源下载脚本
│
└── src/
    ├── main.jsx            # 应用入口
    ├── App.jsx             # 路由（单页）
    ├── index.css           # 全局样式、FAQ、动画等
    │
    ├── pages/
    │   └── HomePage.jsx    # 首页，组装全部区块
    │
    ├── components/         # 页面组件
    │   ├── LoadingScreen.jsx       # 加载动画
    │   ├── Header.jsx              # 顶部固定导航
    │   ├── HeroSection.jsx         # 首屏视频 + 下单栏
    │   ├── DeliveryIcons.jsx       # 配送平台图标
    │   ├── ScrollText.jsx          # 长文滚动逐词动画
    │   ├── AboutBurger.jsx         # 收据视频 + Lottie 标题
    │   ├── BurgersViewer.jsx       # 三款汉堡展示（桌面/移动）
    │   ├── BurgerExtrudeDiagram.jsx # 分解汉堡视频 + SENCILLA 标题
    │   ├── SpecsSection.jsx        # 技术规格标题 + 分栏
    │   ├── SpecificationBlock.jsx  # 规格单栏块
    │   ├── PackagingAnimation.jsx  # 包装盒视频（滚动 scrub）
    │   ├── PriceSection.jsx        # 9.99€ 价格文案
    │   ├── FAQ.jsx                 # 常见问题手风琴
    │   ├── PageTimer.jsx           # 页面停留计时器
    │   ├── ImagesSection.jsx       # 门店 / 服装双图
    │   ├── Footer.jsx              # 橙色页脚
    │   └── OrderModal.jsx          # 下单弹窗
    │
    ├── hooks/
    │   └── useDevice.js    # 移动端 / 平板 / 大屏判断
    │
    └── data/
        ├── content.js      # 文案、FAQ、汉堡数据
        └── lottie/         # meat / chicken / veggie 动画 JSON
```

---

## 四、页面区块顺序

首页自上而下与原站一致：

| 顺序 | 组件 | 内容 |
|------|------|------|
| 1 | `LoadingScreen` | 「Sazonando la carne...」加载动画 |
| 2 | `Header` | PÍDELA / SOBRE LA BURGER / THE ONE |
| 3 | `HeroSection` | 首屏视频 + PARA NO PENSAR + 下单栏 |
| 4 | `ScrollText` | 品牌长文，GSAP 逐词淡入 |
| 5 | `AboutBurger` | 收据滚动视频 + Lottie 动态标题 |
| 6 | `BurgersViewer` | Ternera / Pollo / Veggie 三款汉堡 |
| 7 | `BurgerExtrudeDiagram` | 分解汉堡视频 + SENCILLA 标题 |
| 8 | `SpecsSection` | LAS ESPECIFICACIONES TÉCNICAS + 响应式四栏 |
| 9 | `PackagingAnimation` | 包装盒视频（桌面滚动 pin + scrub） |
| 10 | `PriceSection` | El menú The One cuesta 9.99€... |
| 11 | `FAQ` | 常见问题手风琴 |
| 12 | `PageTimer` | Llevas XX:XXmin en la web... |
| 13 | `ImagesSection` | 灯牌 + 服装图（7/12 + 5/12 横排） |
| 14 | `Footer` | 橙色页脚、联系方式、Legal 链接 |
| — | `OrderModal` | 点击 PÍDELA / Footer 链接弹出 |

---

## 五、技术栈

| 技术 | 用途 |
|------|------|
| React 19 | UI 框架 |
| Vite 8 | 开发与构建 |
| Tailwind CSS 4 | 样式（与原站 class 命名一致） |
| GSAP + ScrollTrigger | 滚动文字、视频 scrub、计时器、包装动画 pin |
| split-type | 文字拆分为词/字动画 |
| lottie-react | AboutBurger 标题动态效果 |
| react-slick | 移动端汉堡轮播 |
| react-router-dom | 路由（当前仅单页 `/`） |
| @vercel/analytics | 访问统计（可选） |

---

## 六、响应式策略

- `useDevice` 钩子区分 **移动端（<768px）**、**平板（768–1024px）**、**大屏（≥1536px）**
- 视频资源按设备切换（如 Hero、收据、分解汉堡、包装盒各有 Desktop / Mobile 版本）
- 技术规格区：`grid-cols-1` → `md:grid-cols-2` → `2xl:grid-cols-4`
- 计时器：桌面右下角固定 + 滚至页脚展开；移动/平板直接显示完整文案

---

## 七、部署到 Vercel

1. 将项目 push 到 GitHub
2. 登录 [vercel.com](https://vercel.com) → **New Project** → 选择仓库
3. Framework 选 **Vite**，直接 Deploy
4. 获得 `xxx.vercel.app` 公网地址

本地也可先验证构建：

```powershell
npm run build
npm run preview
```

---

## 八、已实现功能清单

- [x] 加载动画（文字轮播 + SVG 旋转）
- [x] 固定顶部导航与平滑滚动锚点
- [x] Hero 全屏视频 + 下单栏 + 配送图标
- [x] 长文 ScrollTrigger 逐词动画
- [x] 收据视频滚动驱动（桌面）/ 循环播放（移动）
- [x] Lottie 动态标题（HAY UNA THE ONE...）
- [x] 三款汉堡展示（桌面三列 hover / 移动 slick 轮播）
- [x] 分解汉堡视频 + 响应式大标题
- [x] 技术规格四栏响应式布局 + 文字溢出滚动
- [x] 包装盒视频（桌面 pin + scrub，移动自动播放）
- [x] 9.99€ 价格区块
- [x] FAQ 手风琴（10 题）
- [x] 页面计时器（停留时长 + 已准备数量）
- [x] 双图横排 + 橙色页脚
- [x] 下单弹窗
- [x] 静态资源下载脚本
- [x] 生产构建与 Vercel 部署配置

---

## 九、参考文件

| 文件 | 说明 |
|------|------|
| `reference.js` | 原站打包 JS，用于对照组件与 class |
| `reference.css` | 原站样式参考 |
| `reference-user.html` | 原站 DOM 快照 |

开发时可对照以上文件核对布局、动画参数与文案。

---

## 十、与 AI 协作提示

描述问题时请说明具体组件名或截图位置，例如：

> 「请修改 `PackagingAnimation.jsx` 视频 pin 位置」  
> 「请调整 `SpecsSection` 四栏断点」

这样可以直接定位到对应文件，加快修改效率。
